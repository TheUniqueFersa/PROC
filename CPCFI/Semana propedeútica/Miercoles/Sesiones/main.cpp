#include <bits/stdc++.h>
#define ll long long
#define fastIo() ios_base::sync_with_stdio(0); cin.tie(0); cout.tie(0);
using namespace std;

int gcd(int a, int b){//greatest common divisor
    if(b==0) return a;
    return gcd(b, a%b);
}

ll desc(ll base, ll n){
    ll retorno;
    
    return retorno;
}

int main() {
    fastIo();
    int t = 1;
    
    return 0;
}