#include <iostream>
using namespace std;
#define endl '\n'
#define fori(a,b,c) for(int a = b; a<c; a++)
int main(){
    int n;
    cin >> n;
    string s;
    cin >> s;
    int O = 0, L= 0;
    fori(i, 0, n){
        char c;
        cin >> c;
        if(c=='L') L++;
        else O++;
    }
    int t = (s[0] == 'L')

    fori(){
        
    }
}